{% extends 'base.html.twig' %}

{% block title %}Crud Application{% endblock %}

{% block body %}
<div class="container">
<h2 style="margin-bottom: 40px;
    margin-top: 20px;
    margin-left: 50px;"> Crude Table</h2>
  <table class="table table-striped">
  <thead class="thead-dark">
    <tr>
      <th scope="col">id</th>
      <th scope="col">Title</th>
      <th scope="col">Content</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
  {% for data in data %}
    <tr>
      <th scope="row">{{data.id}}</th>
      <td>{{data.title}}</td>
      <td>{{data.content}}</td>
      <td>
     <a   class="btn btn-success" href="{{ path('update', {'id': data.id}) }}">Update</a>
       <a   class="btn btn-danger" href="{{ path('delete', {'id': data.id}) }}">Delete</a>
    </tr>
   {% endfor %}
  </tbody>
</table>
 <a   class="btn btn-info" href="{{ path('create')}}">create</a>
</div>
{% endblock %}
